# Container Registry

This repo is used to configure and run GitHub Actions to maintain the containers
in the registry:

- `ros`: ROS 2 images from the official Dockerhub registry, but with additional
  quality of life improvements. Non-root user, color terminal, and bash
  completion, making it a much more pleasant experience to develop with
